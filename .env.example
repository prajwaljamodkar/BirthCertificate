# Database connection settings
DB_HOST=db
DB_PORT=5432
DB_NAME=birthcertificate
DB_USER=postgres
DB_PASS=CHANGE_ME

# PostgreSQL container password (used by docker-compose)
POSTGRES_PASSWORD=CHANGE_ME

# Optional: application environment
APP_ENV=production
